{
  "task_id": "p3_fe_blog_fix",
  "phase": 3,
  "agent": "frontend-agent",
  "title": "Fix BlogCard.vue TypeError: author.name is undefined",
  "description": "Fix 'TypeError: can't access property "charAt", o.post.author.name is undefined' on the home page by adding safe access to author properties in BlogCard.vue.",
  "depends_on": ["p3_be_blog_fix"],
  "priority": "high",
  "contracts_required": [".gemini/agents/contracts/api_contracts.md"],
  "acceptance_criteria": [
    "frontend/components/blog/BlogCard.vue uses safe access for post.author.name (e.g. optional chaining or default values)",
    "Avatar placeholder handles missing name gracefully",
    "BlogCard correctly renders when author is present (verified by eye or simple check)",
    "Local linting and type checking passes: npm run lint"
  ]
}
